# xperf workload setting
LOAD=("100M" "150M" "200M" "250M" "300M" "350M" "400M" "450M" "500M" "550M" "600M" "650M" "700M")

# Iteration, duration, and interval. (seconds)
ITERATIONS=3
DURATION=360
INTERVAL=10

# UDP payload
UDP_PAYLOAD=104

# WORKLOAD -- DEBUG ONLY
# LOAD=("140M" "145M")
# ITERATIONS=2
# DURATION=30
# INTERVAL=10